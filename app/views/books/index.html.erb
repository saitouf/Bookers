<h1>Books</h1>

<% @books.each do |book| %>

  <p>Title</p>
  <span><%= book.title %></span>
  
  <p>Body</p>
  <span><%= book.body %></span>
  
  <a>Show</a>
  <a href="/books/edit">Edit</a>
  <!--%= link_to 'Destroy' book_path(@book.id), method: :delite, "deta-confirm" => "Are you sure?" %-->
  
<% end %>


